**Before a figure that carries data or more than a handful of marks, load the `builtin:drawing` skill** — choosing the form (and when not to draw at all), spending the palette, mark specs, labels and legends, and what goes wrong. A two-box sketch does not need it.
