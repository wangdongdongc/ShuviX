// 已下沉 @shuvix/agent-runtime/fileTools/truncate（桌面/扩展共用）。此处再导出兼容既有 import。
export {
  truncateLine,
  truncateHead,
  truncateTail,
  truncateMiddle,
  formatSize,
  DEFAULT_MAX_LINES,
  DEFAULT_MAX_BYTES,
  MAX_LINE_LENGTH
} from '@shuvix/agent-runtime'
