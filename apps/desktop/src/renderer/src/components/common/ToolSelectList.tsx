// 已移至 @shuvix/app-shell（桌面/扩展共用：设置页表单与 frontmatter 属性卡都用它）。
// 此处保留再导出兼容既有 import 路径。
export { ToolSelectList } from '@shuvix/app-shell/common/ToolSelectList'
export type { ToolItem } from '@shuvix/app-shell/common/ToolSelectList'
